# Changelog

## [1.0.5] - 2026-03-12

### Fixed
- 修复 Linux 安装包场景下豆包页面卡在加载状态的问题：`deb` 启动默认增加 `--disable-gpu`。
- 修复 Linux 安装后 sandbox 权限不完整的问题：`postinst` 自动设置 `chrome-sandbox` 为 `root:root` 和 `4755`。
- 修复升级后仍复用旧进程导致行为不一致的问题：`postinst` 增加 `pkill -x aiprotal` 清理旧驻留进程。

### Changed
- 调整运行时策略：默认不再自动启用 `--no-sandbox`，仅在 `AIPROTAL_NO_SANDBOX=1` 时开启。
- 调整开发脚本：`npm run start` / `npm run dev` 默认注入 `AIPROTAL_NO_SANDBOX=1`，避免本地开发受 setuid 约束中断。

## [1.0.0] - 2026-03-12

### Other Changes
- 功能：对齐 Windows/Linux 核心能力并优化侧栏收起体验 (6e7ccb4)
- 修复（窗口）：Linux 下主窗口不显示在任务栏 (b254d07)
- 功能（指标）：优化内存统计范围并允许 keepalive 取值 0-5 (33a1fb1)
- 功能（界面）：重构首页与设置布局并增加实时系统指标 (9c2ca5f)
- 功能（界面）：将助手文案折叠到悬浮信息提示 (2750b52)
- 功能（界面）：更新托盘图标并简化顶部边框 (050fa60)
- 功能(性能): 提升后台拉起速度并新增时延诊断 (62710ff)
- 维护(架构): 采用告警/失败双阈值检查策略 (f2d4be7)
- 功能: 重构设置页与首页并增强运行时可配置能力 (224937b)
- 修复(运行时): 稳定 WebView 切换并降低同步 IO 热点 (e70d52e)
- 修复(UI): 统一外框圆角并优化托盘图标样式 (e541ddf)
- 维护(资源): 固化 portal-4 托盘图标 (39a54c2)
- 功能(主进程): 增加托盘显隐动作与路径感知图标抓取 (043b873)
- 功能(渲染层): 柔化外框圆角并减轻壳层边框 (c7ce2df)
- 修复(WebView): 修正内嵌视口高度并简化截图流程 (a25e76c)
- 修复(可视化): 检测黑屏并清理历史截图产物 (1d5a196)
- 重构(稳定性): 加固运行时与可视化验收流程 (126dcfc)
